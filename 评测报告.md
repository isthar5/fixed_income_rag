#固定收益产品投研问答系统评测报告

## 1. 系统概览

本系统面向基金公司固定收益投研团队，支持对债券研报、利率分析、信用评级报告等文档的问答。

核心特点：

- **业务驱动的 Chunking**：研报按章节/段落拆分，保留上下文逻辑。

- **混合检索链路**：BGE 向量检索 + BM25 → RRF 融合 → Cross-Encoder 重排序 → Top-K 文档送入 LLM。

- **时效性衰减**：根据研报发布日期加权，近期文档权重更高。

- **LLM 生成 + 金融数值校验**：严格引用检索文档，不补充训练知识，数值格式触发重试。

---

## 2. Chunking 策略

### 2.1 策略对比

| 策略       | 描述               | Chunk 数量 | 平均长度 | 备注           |
| -------- | ---------------- | -------- | ---- | ------------ |
| A 通用字符切分 | 基线方案，按固定字符数递归切分  | 245      | 512  | 忽略金融语义       |
| B 标题感知切分 | 按研报章节标题拆分，段落完整保留 | 158      | 820  | 保留上下文逻辑，推荐使用 |

**选择理由**：

- 研报结构明确，按段落/标题切分可保证数值与分析逻辑完整，便于 LLM 理解。

- Chunk 数量较少，平均长度较长，有利于减少检索碎片化带来的信息丢失。

---

## 3. 检索消融实验

### 3.1 平均 Recall@10（Strict vs Relaxed）

| 步骤     | 描述           | Strict Recall@10 | Relaxed Recall@10 | 增益     |
| ------ | ------------ | ---------------- | ----------------- | ------ |
| Step 1 | 向量检索         | 0.775            | 0.775             | -      |
| Step 2 | + BM25 混合    | 0.850            | 0.850             | +0.075 |
| Step 3 | + Rerank 重排序 | 0.950            | 0.950             | +0.100 |
| Step 4 | + 时效性衰减      | 0.950            | 0.950             | 0.000  |

**主要结论**：

- BM25 混合有效提升对精确匹配文档的召回。

- Cross-Encoder Rerank 提升语义排序效果，但需要加权混合防止关键词丢失。

- 时效性衰减设计合理（λ=0.01），未过度压制旧文档。

- Strict 与 Relaxed Recall 趋于一致，说明检索质量较高。

---

## 4. 标注标准说明

人工逐条审查 LLM 回答，与 ground_truth 对比：

- **完全正确**：核心数值/结论一致

- **部分正确**：方向正确但遗漏次要数据

- **错误**：数值错误、逻辑相反或幻觉

---

## 5. 端到端 LLM 生成评估

- **引用准确率**：100%（20/20 条查询的 LLM 回答中，所有引用标注均能在检索到的 Top-5 文档中找到对应来源）。这说明 System Prompt 中“只引用检索文档”的约束生效，未出现幻觉引用。

- **平均重试次数**：0.00 次（数值校验触发极少）。

- **端到端准确率**：

| Query 类型  | 完全正确 | 部分正确 | 错误  |
| --------- | ---- | ---- | --- |
| 简单查询 10 条 | 3    | 2    | 5   |
| 综合查询 10 条 | 2    | 2    | 6   |

---

## 6. Bad Case 分析

### 6.1 AI 如实反映文档内容但被误判

| Query   | 描述           | AI回答     | 审计结论 |
| ------- | ------------ | -------- | ---- |
| 1       | 2月10日10年债收益率 | 1.79%    | 正确   |
| 12      | 美以冲突影响       | 未检索到直接分析 | 正确   |
| 13      | 06国债09估值净价   | 2月11日未提及 | 正确   |
| 14      | 5月降息幅度       | 未明确提及    | 正确   |
| 15 & 16 | 企业违约案例       | 未提及具体名称  | 正确   |
| 19      | 4月7日利差       | 缺乏当日数据   | 正确   |

### 6.2 幻觉错误（AI 胡编或逻辑错位）

| Query                  | 问题描述           | 审计结论                 |
| ---------------------- | -------------- | -------------------- |
| 5                      | 2025年债券发行总额    | 系统声称未提及，但研报明确有数据     |
| 6                      | 2025末AAA 3年期利率 | 系统引用2026数据，实际应为2.04% |
| 7                      | 2月11日下破1.80%逻辑 | 使用未来数据解释过去，时空错位      |
| 2,4,8,9,10,11,17,18,20 | 精确数值编造         | 幻觉                   |

---

## 7. 扩展性思考

- **实时行情接入**：时效性衰减需调整为秒级或分钟级 λ，增大动态权重衰减幅度。

- **MMR 多样性重排**：可在 Rerank 后调用，保证跨文档多样性，减少重复信息。

- **结构化+语义混合查询**：对具体数值查询先做字段过滤，再进行语义检索，提升精确度。

- **LightGBM Reranker**：可替代 Cross-Encoder，兼顾速度与准确率。

---

## 8. 本地环境限制说明

本地开发环境仅 16GB 内存，同时运行 Docker/Qdrant/Embedding 模型导致部分检索请求因内存不足失败。本报告中消融实验数据已在受限环境下完成，预期在服务器环境（32GB+）可获得更稳定结果。

---

## 9. 总结

- Chunking 策略按标题拆分有效提升信息完整性。

- 混合检索 + Rerank + 时效性衰减组合，端到端 Recall@10 可达 0.95。

- LLM 输出在绝大多数情况下准确引用文档，数值校验触发率低。

- 幻觉主要出现在缺少日度数据或系统逻辑处理不当的场景，需在训练或系统设计中进一步约束。

- 系统设计具备可扩展性，可支持实时行情、多样性重排和结构化查询增强。


